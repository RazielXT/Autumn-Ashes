#pragma once

struct ObjectSelectionEditor
{
	/*
			Ogre::Entity* result = nullptr;
		Ogre::Vector3 hitpoint;

		if (PickEntity(Ogre::Ray(pCamera->getPosition(), pCamera->getFacingDirection()), &result, hitpoint, "", 100000))
			GUtils::DebugPrint(result->getName());
			*/
};